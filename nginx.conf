user  nginx;

events {
    worker_connections   1000;
}
http {
        server {
              listen 4000;
              location / {
                proxy_pass http://loginapp:80;
			    proxy_pass http://sqlserverService:1433;
              }
        }
}