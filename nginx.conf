user  nginx;

events {
    worker_connections   1000;
}

 http{
        server {
              listen 4000;
              location / {
                proxy_pass http://loginapp:80;

              }
        }
		
		 server {
              listen 1433;
              location / {
                proxy_pass http://loginapp:1433;

              }
        }
		
}